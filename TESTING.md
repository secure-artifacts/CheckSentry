# CheckSentry 发布前验收清单

本文用于在 Windows 10/11、Windows PowerShell 5.1 与 PowerShell 7 环境完成最终验收。每轮测试都应从发布包重新解压，并保留失败时的控制台输出、浏览器开发者工具错误和脱敏后的测试工作簿。

## 已完成的非 Windows 验证

| 项目 | 结果 |
|---|---|
| 三个 PowerShell 脚本解析 | 通过，0 个语法错误 |
| PowerShell 5.1 兼容语法规则 | 通过，未发现 7.x 专用语法 |
| PSScriptAnalyzer | 0 个错误；仅保留控制台输出、内部函数命名等风格提示 |
| UTF-8 BOM | 三个 `.ps1` 及两个 HTML 模板均恰好一个 BOM |
| Excel 模板结构 | 通过；仅“说明、白名单、待定、黑名单”四张表，规则表为空 |
| Excel 事务 | 新增、编辑、删除、跨表移动、批量写入与单步撤销通过 |
| 并发保护 | 工作簿在操作期间变化时取消写入；撤销遇到外部修改时拒绝覆盖 |
| 公式注入 | 以 `=HYPERLINK(...)` 开头的备注按文本保存；既有公式工作簿被拒绝 |
| HTML/XSS | `<script>`、引号、事件属性片段、反斜杠、换行和中文按文本编码 |
| HTTP 写接口 | 非 JSON、错误令牌和跨源 Origin 被拒绝；正文大小有限制 |
| 规则优先级 | 黑名单高于白名单，白名单高于待定 |

## Windows 虚拟机测试矩阵

至少完成以下组合：

1. Windows 10/11 x64 + Windows PowerShell 5.1。
2. Windows 10/11 x64 + 当前 PowerShell 7 LTS。

每个组合都执行首次运行、重复扫描、清单维护、撤销和端口占用测试。

## 必测场景与预期结果

### 1. 空白模板首次运行

- 准备：目录中仅保留发布文件，不放 `list.xlsx`。
- 操作：启动工具并等待报告页出现。
- 预期：从 `list_template.xlsx` 创建 `list.xlsx`；四张工作表和列名不变；扫描到的新对象仅新增到“待定”；页面无白屏。

### 2. `list.xlsx` 不存在时自动创建

- 操作：删除测试副本中的 `list.xlsx` 后再次启动。
- 预期：重新从模板复制，模板本身不被改写；创建失败时显示明确错误，不产生半写入文件。

### 3. Excel 文件被占用

- 操作：用 Excel 打开 `list.xlsx`，在报告或 `/manage` 执行新增、编辑、删除、批量分类和撤销。
- 预期：操作失败并提示关闭 Excel；原文件、规则、工作表和上一步可撤销状态不被破坏。关闭 Excel 后重试成功。

### 4. 重复规则

- 准备：在同一表放入两个相同对象规则，再在不同表放入相同对象规则；规则 ID 保持唯一。
- 预期：控制台报告同表重复或跨表冲突，不自动删除或覆盖任何行；实际判定仍为黑名单 → 白名单 → 待定。同一表内按原始行顺序取首条命中。
- 复合身份：分别加入 `VeraCrypt / 1.25.9 / IDRIX`、`VeraCrypt / 1.26.24 / AM Crypto`，以及两个不同发布者的 `4K Video Downloader+`，预期全部保留；安装路径不同或相同都不得影响规则去重。仅名称、版本、发布者完全相同的软件规则才算真正重复。
- 额外：制造重复 ID 后启动，预期工具拒绝读取并指出工作表和重复 ID。

### 5. 添加时间为空或非法

- 准备：把既有规则的“添加时间”留空、写入普通非法日期文本。
- 预期：扫描和页面加载不崩溃，值作为文本显示/保留；新写入规则使用 `yyyy-MM-dd HH:mm`。

### 6. 同一对象同时存在三类清单

- 预期：只显示“命中黑名单”；删除黑名单规则后命中白名单；再删除白名单规则后才命中待定。

### 7. 新软件自动加入待定

- 操作：安装一个未登记软件或临时添加对应卸载注册表测试项，连续刷新两次。
- 预期：第一次只新增一条“待定”，第二次不重复；不影响任何白名单或黑名单规则；自动写入使之前的网页撤销快照失效。

### 8. 编辑后撤销

- 操作：修改关键词、版本、发布者、分类和备注，保存后点击“撤销上一步”。
- 预期：整行恢复为修改前值，其他规则不变，撤销按钮随后禁用。

### 9. 删除后撤销

- 预期：被删规则恢复到原工作表，所有字段完整，其他规则不变。

### 10. 批量分类后撤销

- 操作：选择多个软件和插件，批量移到白名单或黑名单，然后撤销。
- 预期：一次撤销恢复整个批次；请求中若包含重复对象，服务器拒绝整个批次且工作簿不变。
- 性能：在待定表至少 500 条规则时选择 20 条批量分类，记录从点击确认到成功提示的时间；普通本地磁盘目标不超过 3 秒。页面更新不应再次扫描浏览器；点击“重新扫描”才执行完整扫描。

### 11. 撤销与外部修改冲突

- 操作：网页完成一次维护后，用 Excel 修改并保存 `list.xlsx`，再点击撤销。
- 预期：撤销被拒绝，Excel 的外部修改不被覆盖。

### 12. Firefox 只有内置插件

- 预期：内置/系统扩展不作为用户插件列出；没有用户插件时返回空列表且报告正常。

### 13. 多浏览器、多个人资料

- 准备：为 Chromium 系浏览器和 Firefox 建立多个个人资料并安装同一/不同扩展。
- 预期：Chrome、Edge、Chromium、Brave、Vivaldi、Naver Whale、Firefox 均被识别；同一扩展合并展示并列出浏览器/个人资料；不同扩展不串项。

### 14. 损坏或特殊 manifest

- 准备：测试缺少图标、损坏 JSON、多个版本目录，以及名称为 `__MSG_xxx__` 的扩展。
- 预期：损坏版本被跳过并继续扫描；选择可解析的最高版本；国际化名称优先按默认/当前语言解析，无法解析时回退到扩展 ID；缺图标时使用安全占位图标。

### 15. 特殊字符与公式注入

- 输入：备注或字段包含 `<script>`、单双引号、反斜杠、换行、中文，以及以 `=`, `+`, `-`, `@` 开头的内容。
- 预期：网页只显示文本，不产生脚本、事件处理器或破坏页面结构；写入 Excel 后单元格没有公式；复制 TSV 时危险前缀自动加单引号。

### 16. 端口 8787 被占用

- 操作：先让其他程序监听 8787，再启动工具。
- 预期：自动尝试后续端口并在控制台显示实际 URL；浏览器打开实际端口。若连续候选端口均不可用，显示明确错误并退出，不残留监听器。

### 17. 注册表覆盖范围

- 准备：分别在 HKLM/HKCU、32 位/64 位卸载视图放置测试项。
- 预期：四个来源均可扫描；不会因 32/64 位进程不同而漏读或重复读取 `Wow6432Node`；名称为空的条目被忽略；安装路径和图标缺失不导致扫描失败。

### 17.1 软件图标资源

- 准备：选择 `DisplayIcon` 分别为 `app.exe,0`、`resource.dll,12`、`resource.dll,-12`、带空格/引号/环境变量路径、路径不存在和字段为空的软件。
- 预期：有效的 EXE/DLL 资源索引显示对应图标；普通 PNG/JPEG/GIF/ICO 继续显示；无效路径或资源依次安全回退到 MSI `ProductIcon`、安装目录、开始菜单快捷方式图标/目标程序或占位图标；重复刷新不产生 GDI 句柄持续增长。
- MSI 空字段：使用 `DisplayIcon` 和 `InstallLocation` 均为空、卸载命令为 `MsiExec.exe /X{GUID}` 的软件；若同名开始菜单快捷方式指向有效 EXE，应显示该目标的图标。候选同分或名称无关时必须使用占位图标，不能误配其他软件图标。
- 实机核对：至少检查 Microsoft Edge、Cloudflare WARP、Oracle VirtualBox、NVIDIA 组件和 VB-CABLE，并与 Windows“应用和功能”或 Revo 的图标进行对照。

### 18. ImportExcel 异常

- 操作：分别测试模块不存在、无网络、安装权限不足、存在其他版本和已安装 7.8.10。
- 预期：仅加载已验证的 7.8.10；缺失时尝试当前用户安装；失败时给出可执行的手动安装提示；不调用 Office COM。

### 19. 浏览器无法自动打开与 HTTP 失败

- 预期：自动打开失败时控制台打印可手动访问的 URL；接口错误返回 JSON/HTML 错误而非白屏；刷新后仍可继续使用。

### 20. 备注/原因超链接

- 准备：在 `备注/原因` 中分别输入纯文本 `https://docs.google.com/spreadsheets/d/...`，以及在 Excel/LibreOffice 中设置显示文字为“安全依据”的原生超链接。
- 预期：报告页和清单维护页中超链接将被识别为可点击链接，在新标签页打开；长文本和长链接通过原生 `title` 属性在鼠标悬停时显示完整内容；`javascript:`、`file:` 和其他非 HTTP/HTTPS 协议只按普通文本处理；在本地 Excel 中编辑其他字段或跨表移动规则时，原生超链接不丢失。

### 21. Google Sheets 只读模板

- 准备：建立包含“白名单、待定、黑名单”三个工作表的 Google Sheets，设置为目标用户可查看并复制其 `/edit` 链接到维护页或报告页的“云端清单设置”。这三个表必须包含 B 列至 K 列的 10 个现有表头，A 列和 L 列及以后可自由填写管理内容。如果出现同一个插件 ID 在多处重复，同步应当明确报错并中止。
- 预期：保存链接后点击“保存并同步”或重新启动工具，云端规则下载并合并到本地 `list.xlsx`；云端规则将无条件覆盖本地同身份规则的所有字段（包括备注和添加人）；云端没有的本地私有规则保留；网络不可用、链接无权限或表结构错误时跳过同步并继续使用本地清单。注意：为了解决 Google Sheets 导出 XLSX 时带有的未声明 XML 前缀导致的解析错误，云端同步时会强力剥离所有原生超链接，云端维护的超链接必须以纯文本形式填写，前端 UI 会自动将其转换为可点击链接。
- 撤销隔离：维护页点击“立即同步”可撤销；应用启动时的自动同步不会消耗已有的人工撤销快照。
- 版本替换：先同步云端旧版本，再把云端同一软件改为新版本并同步；预期旧云端身份被移除、新身份写入，本地私有规则不受影响。
- 隐私：抓包或检查请求确认只发出 Google Sheets 导出请求，不上传软件名称、插件、版本、安装路径、用户名、添加时间或报告内容。

## 发布前最后检查

- 在 Windows PowerShell 5.1 执行 `tests\Run-Tests.ps1`，必须全部通过。
- 双击 `CheckSentry.exe` 后，浏览器应在 2 秒左右显示扫描进度页；扫描期间进度接口持续可响应。
- 在 300 个以上软件的电脑上确认首屏扫描阶段不递归开始菜单、不批量提取 EXE/DLL 图标；滚动报告时图标才逐步加载。
- Chromium 已禁用插件和仅残留在 Extensions 目录、但 Preferences/Secure Preferences 没有记录的插件默认不进入报告。
- 同一插件在两个配置文件分别为 `1.2`、`1.3` 时，白名单 `1.3` 或 `1.*` 均能正确匹配。
- 正式发布的 `CheckSentry.exe` 使用 `Get-AuthenticodeSignature` 或 `signtool verify /pa /v` 验证为有效签名。
- 用全新解压目录完成 Windows PowerShell 5.1 和 PowerShell 7 两轮测试。
- 确认 GitHub/压缩包中不存在 `list.xlsx`、锁文件、个人路径、备份、日志、测试结果和临时文件。
- 校验发布压缩包 SHA-256 与随附校验文件一致。
- 不要把测试过程中生成的个人 `list.xlsx` 复制回源码或发布目录。
